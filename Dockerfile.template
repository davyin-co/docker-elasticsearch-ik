ARG ES_VERSION
# https://github.com/elastic/elasticsearch-docker
FROM elasticsearch:${ES_VERSION}
ARG ES_VERSION

# https://github.com/medcl/elasticsearch-analysis-ik/releases
ENV IK_RELEASE=https://github.com/medcl/elasticsearch-analysis-ik/releases/download/v${ENV_ES_VERSION}/elasticsearch-analysis-ik-${ENV_ES_VERSION}.zip
RUN echo ${ES_VERSION} && echo $IK_RELEASE && echo ${IK_RELEASE}
RUN /usr/share/elasticsearch/bin/elasticsearch-plugin install --batch $IK_RELEASE
